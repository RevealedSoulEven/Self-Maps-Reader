## Self-Maps-Reader

simple app to read `/proc/self/maps` and  `/proc/self/` for debugging and learning purpose.

- check libc.so integrity on disk and memory
- check libart.so integrity on disk and memory
